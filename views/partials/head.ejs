<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="theme-color" content="#0A0A0F">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <link rel="icon" type="image/svg+xml" href="/1048852.svg">
<title><%= title || "FitTrack" %></title>

    <!-- Google Fonts - Montserrat -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700;800;900&display=swap" rel="stylesheet">

    <!-- Tailwind CDN -->
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <!-- Custom Styles - NEON ENERGY THEME -->
    <style>
        :root {
            --neon-green: #0AFF9D;
            --cyan-glow: #00E0FF;
            --deep-black: #0A0A0F;
            --dark-gray: #15151F;
        }

        * {
            -webkit-tap-highlight-color: transparent;
        }

        body {
            font-family: 'Montserrat', -apple-system, BlinkMacSystemFont, sans-serif;
            overflow-x: hidden;
            background-color: var(--deep-black);
            color: #FFFFFF;
        }

        /* Bold Typography */
        h1, h2, h3, h4, h5, h6 {
            font-weight: 800;
            letter-spacing: -0.02em;
        }

        /* Smooth transitions */
        .smooth-transition {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        /* Card hover effects with glow */
        .card-hover {
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .card-hover:hover {
            transform: translateY(-2px);
            box-shadow: 0 0 20px rgba(10, 255, 157, 0.3);
        }

        .card-hover:active {
            transform: scale(0.98);
        }

        /* Neon Glow Effects */
        .glow-green {
            box-shadow: 0 0 15px rgba(10, 255, 157, 0.5),
                        0 0 30px rgba(10, 255, 157, 0.3),
                        0 0 45px rgba(10, 255, 157, 0.1);
        }

        .glow-cyan {
            box-shadow: 0 0 15px rgba(0, 224, 255, 0.5),
                        0 0 30px rgba(0, 224, 255, 0.3),
                        0 0 45px rgba(0, 224, 255, 0.1);
        }

        .text-glow-green {
            text-shadow: 0 0 10px rgba(10, 255, 157, 0.8),
                         0 0 20px rgba(10, 255, 157, 0.5),
                         0 0 30px rgba(10, 255, 157, 0.3);
        }

        .text-glow-cyan {
            text-shadow: 0 0 10px rgba(0, 224, 255, 0.8),
                         0 0 20px rgba(0, 224, 255, 0.5),
                         0 0 30px rgba(0, 224, 255, 0.3);
        }

        /* Neon borders */
        .border-neon-green {
            border-color: var(--neon-green);
        }

        .border-cyan {
            border-color: var(--cyan-glow);
        }

        /* Backgrounds */
        .bg-neon-green {
            background-color: var(--neon-green);
        }

        .bg-cyan {
            background-color: var(--cyan-glow);
        }

        .bg-deep-black {
            background-color: var(--deep-black);
        }

        .bg-dark-gray {
            background-color: var(--dark-gray);
        }

        /* Text colors */
        .text-neon-green {
            color: var(--neon-green);
        }

        .text-cyan {
            color: var(--cyan-glow);
        }

        /* Input focus effects with glow */
        input:focus, select:focus, textarea:focus {
            outline: none;
            border-color: var(--neon-green);
            box-shadow: 0 0 10px rgba(10, 255, 157, 0.5);
        }

        /* Custom scrollbar - Neon style */
        ::-webkit-scrollbar {
            width: 10px;
        }

        ::-webkit-scrollbar-track {
            background: var(--dark-gray);
        }

        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, var(--neon-green), var(--cyan-glow));
            border-radius: 5px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(180deg, var(--cyan-glow), var(--neon-green));
        }

        /* Slide up animation */
        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .slide-up {
            animation: slideUp 0.5s ease-out;
        }

        /* Pulse animation for buttons */
        @keyframes pulse-glow {
            0%, 100% {
                box-shadow: 0 0 15px rgba(10, 255, 157, 0.5);
            }
            50% {
                box-shadow: 0 0 25px rgba(10, 255, 157, 0.8);
            }
        }

        .pulse-glow {
            animation: pulse-glow 2s ease-in-out infinite;
        }

        /* Gradient backgrounds */
        .gradient-neon {
            background: linear-gradient(135deg, var(--neon-green), var(--cyan-glow));
        }

        .gradient-dark {
            background: linear-gradient(135deg, var(--dark-gray), var(--deep-black));
        }

        /* Fixed chart container */
        .chart-container {
            position: relative;
            height: 250px;
            width: 100%;
        }

        /* High contrast buttons */
        .btn-neon {
            background: var(--neon-green);
            color: var(--deep-black);
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            border: 2px solid var(--neon-green);
            box-shadow: 0 0 15px rgba(10, 255, 157, 0.5);
            transition: all 0.3s ease;
        }

        .btn-neon:hover {
            background: transparent;
            color: var(--neon-green);
            box-shadow: 0 0 25px rgba(10, 255, 157, 0.8);
            transform: translateY(-2px);
        }

        .btn-cyan {
            background: var(--cyan-glow);
            color: var(--deep-black);
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            border: 2px solid var(--cyan-glow);
            box-shadow: 0 0 15px rgba(0, 224, 255, 0.5);
            transition: all 0.3s ease;
        }

        .btn-cyan:hover {
            background: transparent;
            color: var(--cyan-glow);
            box-shadow: 0 0 25px rgba(0, 224, 255, 0.8);
            transform: translateY(-2px);
        }

        /* Card styles with dark background and neon accents */
        .neon-card {
            background: var(--dark-gray);
            border: 1px solid rgba(10, 255, 157, 0.2);
            border-radius: 12px;
            transition: all 0.3s ease;
        }

        .neon-card:hover {
            border-color: var(--neon-green);
            box-shadow: 0 0 20px rgba(10, 255, 157, 0.3);
        }
    </style>
</head>

<body class="bg-deep-black text-white min-h-screen flex flex-col">
